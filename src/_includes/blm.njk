
<div class="{% if site.fullheighthome %}-mt-25p{% elseif page.url == 'contact' %}mt-0{% else %}mt-12{% endif %}">

    <!-- blm yellow -->
    <div class="">
        <a href="{% if page.url == '/acknowledgement-of-country/' %}/who/{% else %}/acknowledgement-of-country/{% endif %}" class="relative z-10 bg-yellow mx-auto h-64 w-64 -mb-32 rounded-full flex items-center text-center justify-center text-xl text-black font-bold hover:shadow-sm hover:text-red transition duration-150 ease-in-out">
            <!-- icon -->
            {% if page.url !== '/' and page.url !== '/acknowledgement-of-country/' %}
                <svg  class="text-black h-6 w-6 mr-1" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor"><path d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path></svg>
            {% endif %}
            {% if page.url == '/acknowledgement-of-country/' %}
                <span class="font-serif text-4xl">stoqe</span>
            {% else %}
                #BlackLivesMatter
            {% endif %}
        </a>
    </div>

    <!-- blm red -->
    <div class="relative z-0 bg-red pb-6">
        <div class="max-w-screen-xl mx-auto h-m py-4 px-3 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between flex-wrap">
                <div class="w-0 flex-1 flex items-center h-24">

                </div>
            </div>
        </div>
    </div>

</div>